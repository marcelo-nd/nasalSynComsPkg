% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nasalPkgFunctions.R
\name{read_metadata}
\alias{read_metadata}
\title{Read metadata table from CSV}
\usage{
read_metadata(path, sort_table = FALSE)
}
\arguments{
\item{path}{Character. Path to the metadata CSV file.}

\item{sort_table}{Logical. If TRUE, metadata rows are sorted alphabetically
by their row names (default = FALSE).}
}
\value{
A data.frame containing the metadata, with row names assigned from
the first column of the CSV file.
}
\description{
Loads a metadata table from a CSV file, optionally sorting it by row names.
The first column of the CSV file is treated as row names (typically sample IDs).
}
\details{
The function expects the CSV file to have:
\itemize{
\item A header row
\item A first column containing unique sample identifiers
}
}
\examples{
\dontrun{
# Read metadata as-is
md <- read_metadata("metadata.csv")

# Read metadata and sort by sample names
md_sorted <- read_metadata("metadata.csv", sort_table = TRUE)
}
}
